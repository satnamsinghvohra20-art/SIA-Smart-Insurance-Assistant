version: '3.8'

services:
  claimpilot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claimpilot-app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=8000
    restart: unless-stopped
